# Runtime — full stack including the browser renderer.
# Pins reviewed monthly per docs/architecture/scrape-service.md (U6 runbook).
fastapi==0.115.12
uvicorn[standard]==0.34.2
httpx==0.28.1
beautifulsoup4==4.15.0
crawl4ai==0.9.2
# Explicit: snapshots.py re-encodes over-budget screenshots (crawl4ai pulls it in too).
pillow==12.3.0
render_sdk==0.7.0
